[data_loader]
header_file=header_groupings.json

[flask]
# Default port & host should be fine
host=localhost
port=8080

[mongodb]
# Default port & host should be fine
host=localhost
port=27017
# Database name
db=samples